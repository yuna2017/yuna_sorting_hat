# 贡献指南

感谢对 YUNA 分部帽的改进建议。

## 开发环境

- Node.js 22 或更高版本；
- npm；
- 如果要执行浏览器视觉检查，需要 Microsoft Edge；Playwright 已列为开发依赖。

## 提交前检查

```bash
npm ci
npm run verify
npm run check:browser
```

其中：

- `npm run verify:quality`：单元测试、部门内容检查和题库分布模拟；
- `npm run verify`：在质量检查后继续执行本地生产构建；
- `npm run check:browser`：完整浏览器流程、移动端尺寸、分享海报和四部门结果检查。

## 修改规则

- 修改题目、权重、槽位或分享协议前，先阅读 `docs/题库规范.md`；
- 修改页面流程、结果页结构或部署方式前，先阅读 `docs/设计文档.md`；
- 不要提交个人联系方式、内网地址、测试凭据或未确认的素材；
- 不要在判定流程中引入随机决胜；
- 修改公共招新链接、二维码或活动信息时，需要同步更新发布记录。

## Pull Request 建议

Pull Request 请说明：

1. 修改目的；
2. 影响的页面、题库或分享协议；
3. 是否需要升级题库版本；
4. 已执行的测试命令；
5. 是否需要真机或线上环境复核。
