---
authors:
  - Benincasamatch
name: TODO
---

# 待办清单

本轮 MVP 已完成：封面 → 开场 → 答题 → 结果（雷达图 + 四项读数），
移动优先，GitHub Pages 自动部署。以下是剩下的事，按优先级排。

图例：`[ ]` 未开始 · `[~]` 部分完成 · `[x]` 完成

---

## 一、上线前必须做（阻塞招新使用）

这些不做，页面上线了也没法真正拉到人。

- [ ] **填四个部门的介绍文案** —— `src/data/departments.ts` 里的 `intro`
  - 现在是 `null`，结果页不渲染该区块（不会留空白，但也就没有介绍）
  - 需要社团自己写，我没有编造事实
  - 开发模式下控制台会列出还缺哪些
- [ ] **填招新链接** —— `src/data/departments.ts` 里的 `links`
  - 格式 `{ label: '招新群', href: '...' }`，空数组时结果页不渲染链接区
  - 可以是招新群二维码页、部门作品集、报名表单
- [ ] **确定招新二维码指向的 URL** —— 分享卡要用，也可先放进 `links`
- [ ] **开启 GitHub Pages** —— 仓库 Settings → Pages → Source 选 **GitHub Actions**
  - 不开这一项，`deploy.yml` 跑完也不会真的发布
- [ ] **真机各测一次** iOS Safari 与 Android Chrome
  - 两者的浏览器工具栏高度行为不同，`dvh` 表现要亲眼确认
  - 我只在 Edge + 设备模拟里验过 390px 与 320px

## 二、下一轮功能

- [ ] **分享卡 UI + 招新二维码**
  - 接缝已就位：`src/lib/shareCode.ts` 的 `encodeAnswers` / `buildShareUrl`
  - `?a=<10位码>` 已能完整复现结果，刷新与换设备都一致
  - 可复用 `DeptCard`（立绘）与 `data-dept` 换肤，版式和二维码是新工作
  - 二维码库建议选体积小的，别为一个二维码背进一个大依赖
- [ ] **结果揭示仪式（过渡屏）**
  - 在 `App.tsx` 的 `phase` 状态机里于 `quiz` 与 `result` 之间加一个阶段
  - 素材已有：分部帽 SVG 可做「帽子沉思」动效
- [ ] **分享按钮**（复制链接 / 保存图片）

## 三、打磨（可选，不影响可用）

- [ ] **换最终视觉素材**
  - 分部帽目前是手绘 SVG 占位，见 `src/components/SortingHat.tsx`，换掉只动这一个组件
  - 若要换字体：**必须自托管 woff2 打进产物**，不要用 CDN 外链
    （`fonts.googleapis.com` 在国内不可达，会白屏）；
    `--font-display` 已把 `'Cinzel'` 放首位，丢进字体文件即生效
- [ ] **音效** —— 设计文档标注「不必要」，想做再做
- [ ] **压缩立绘** —— 每张约 130KB，只加载冠军那一张，所以不急；
  真要压可以转 AVIF 或降尺寸
- [ ] **雷达图入场动画** —— 多边形从中心展开，会更有仪式感

## 四、我没验完的一项

- [ ] **在浏览器里实测 dev / sec / pr 三套主题的渲染**
  - `ops` 我逐项看过了（主题、立绘、雷达、读数都正常）
  - 另三套：写好了脚本但执行环境的安全分类器当时持续不可用，没跑成
  - 只读手段能确认的已确认：四个 `[data-dept]` 主题块带正确色值编进了产物 CSS、
    四张立绘都在 `dist` 里、单测证明四条分享码各自判给正确部门
  - **补验方法**：
    ```bash
    npm i --no-save playwright
    node check-depts.mjs
    ```
    或直接手动访问这四个链接（本地 dev 或线上都行）：

    | 部门 | 分享码 |
    | --- | --- |
    | 开发部 | `?a=acbddbabad` |
    | 网安部 | `?a=cadbbdcadb` |
    | 运维部 | `?a=bdacacbdcc` |
    | 组宣部 | `?a=dbcacadcba` |

## 五、维护约定

改题目时请记住：

- [x] 加题不需要改判定逻辑 —— 满分自动变成 `题数 × 3`
- [x] 决胜题由数据里的 `decider: true` 标记，不是硬编码题号
- 每题必须满足「4 个选项各主推一个不同部门 + 4 个副推构成无固定点置换」，
  否则结果页会失衡。**改完务必跑 `npm test`** —— 有校验器守着这条，
  开发模式下控制台也会报。
- 归一化除的是单部门满分（30），**不是总分**。除总分会把雷达四轴压扁。
- `docs/*.md` 是唯一信息源（`.docx` 副本已删除，避免两边各改一份而分叉）。
